import React, { FunctionComponent } from 'react'
import styles from './module.NewBadge.scss'

export const NewBadge: FunctionComponent = () => {
  return <small className={styles.newBadge}>New</small>
}
