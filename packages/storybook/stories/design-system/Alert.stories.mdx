import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks'

import { action } from '@storybook/addon-actions'
import { Alert } from '@frontend/design-system'

export const alertStyles = ['info', 'success', 'warning', 'danger']

# Alert

<Meta title="Design system/Alert" component={Alert} />

## Basic

<Preview>
  <Story name="Basic">
    {alertStyles.map((style) => (
      <Alert bsStyle={style} onDismiss={action('dismissed')}>
        A(n) {style} alert
      </Alert>
    ))}
  </Story>
</Preview>

# Props

<Props of={Alert} />
