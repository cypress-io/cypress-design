import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks'
import { capitalize } from 'lodash'

import { Button, Panel } from '@frontend/design-system'

export const panelStyles = [
  '',
  'primary',
  'info',
  'success',
  'warning',
  'danger',
]

# Panel

<Meta title="Design system/Panel" component={Panel} />

## Basic

<Preview>
  <Story name="Basic">
    {panelStyles.map((style) => (
      <div style={{ marginBottom: '16px', width: '700px' }} key={style}>
        <Panel bsStyle={style}>
          <Panel.Heading>
            <Panel.Title>{capitalize(style) || 'Default'} panel</Panel.Title>
          </Panel.Heading>
          <Panel.Body>
            <p>
              Ut exercitationem consequatur quae sit architecto officia cumque.
            </p>
            <div>
              <Button bsStyle={style}>Button</Button>
            </div>
          </Panel.Body>
        </Panel>
      </div>
    ))}
  </Story>
</Preview>

# Props

<Props of={Panel} />
