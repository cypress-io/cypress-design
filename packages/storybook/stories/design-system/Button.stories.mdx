import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks'

import { Button } from '@frontend/design-system'
import { action } from '@storybook/addon-actions'

export const buttonStyles = [
  'Default',
  'Primary',
  'Danger',
  'Light',
  'Dark',
  'Error',
  'Link',
]

# Button

<Meta title="Design system/Button" component={Button} />

## Basic

<Preview>
  <Story name="Basic">
    {buttonStyles.map((style) => {
      return (
        <Button
          bsStyle={style.toLowerCase()}
          onClick={action('clicked')}
          style={{ margin: '0 16px 16px 0' }}
        >
          {style} Button
        </Button>
      )
    })}
  </Story>
</Preview>

## Outline

Stacks well with the various `bsSize` options

<Preview>
  <Story name="Outline">
    {buttonStyles.map((style) => {
      return (
        <Button
          bsStyle={style.toLowerCase()}
          className="btn-outline"
          onClick={action('clicked')}
          style={{ margin: '0 16px 16px 0' }}
        >
          {style} Button
        </Button>
      )
    })}
  </Story>
</Preview>

## Tiny

<Preview>
  <Story name="Tiny">
    {buttonStyles.map((style) => {
      return (
        <Button
          bsSize="xs"
          bsStyle={style.toLowerCase()}
          onClick={action('clicked')}
          style={{ margin: '0 16px 16px 0' }}
        >
          {style} Button
        </Button>
      )
    })}
  </Story>
</Preview>

## Small

<Preview>
  <Story name="Small">
    {buttonStyles.map((style) => {
      return (
        <Button
          bsSize="sm"
          bsStyle={style.toLowerCase()}
          onClick={action('clicked')}
          style={{ margin: '0 16px 16px 0' }}
        >
          {style} Button
        </Button>
      )
    })}
  </Story>
</Preview>

## Large

<Preview>
  <Story name="Large">
    {buttonStyles.map((style) => {
      return (
        <Button
          bsSize="lg"
          bsStyle={style.toLowerCase()}
          onClick={action('clicked')}
          style={{ margin: '0 16px 16px 0' }}
        >
          {style} Button
        </Button>
      )
    })}
  </Story>
</Preview>

# Props

<Props of={Button} />
