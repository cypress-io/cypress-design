{
	"$schema": "https://turborepo.org/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": [
				"^build"
			],
			"inputs": [
				"*.@(vue|ts|tsx)",
				"../*.ts"
			],
			"outputs": [
				"dist/**"
			]
		}
	}
}