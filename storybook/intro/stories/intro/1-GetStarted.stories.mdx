import { Meta } from '@storybook/addon-docs';

<Meta
  title="Get Started"
/>

# Cypress Design System

<p className="text-2xl max-w-200">
  Welcome to the Cypress UI toolkit!
</p>
<p className="text-2xl max-w-250">
  This will help you build apps & websites respecting Cypress branding guidelines.<br/>
  <ul>
    <li className="text-2xl">Find Components for both React and Vue</li>
    <li className="text-2xl">And tools tested in Vite and Webpack</li>
  </ul>
  Let's get coding! Here's how to start:
</p>

<ul className="list-none p-0 flex flex-col gap-4 my-10 max-w-200">
  {
    [{
      title: 'Read the install documentation',
      href: '/docs/install--page',
      logo: require('../../../logos/vite.svg')
    },
    {
      title: 'See components for React',
      href: '/docs/react_index--page',
      logo: require('../../../logos/react.svg')
    },
    {
      title: 'See components for Vue',
      href: '/docs/vue_index--page',
      logo: require('../../../logos/vue.svg')
    }].map(({ title, href, logo }) => (
      <li>
        <a className="text-xl block p-4 border border-gray-200 rounded flex items-center bg-white hover:bg-jade-50 transition" href={href}>
          <img src={logo} className="w-8 m-4"/>
          <span className="flex-1">{title}</span>
        </a>
      </li>
    ))
  }
</ul>

