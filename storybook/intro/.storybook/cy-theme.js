import { create } from '@storybook/theming'
import logo from './public/logo.svg'

export default create({
  base: 'light',
  brandTitle: 'Cypress Design',
  brandUrl: 'https://github.com/cypress-io/cypress-design',
  brandImage: logo,
})
