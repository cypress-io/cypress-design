---
layout: '../../layouts/BaseLayout.astro'
title: Button Bar
order: 2
---

import ButtonBar from '../../demos/ButtonBar.astro'

# Button Bar

## Usage

If you have to make a button bar, you can use the `flex` and `gap` classes to make it look nice.

<div class="border border-gray-100 rounded-lg my-4 overflow-hidden">
  <ButtonBar />
</div>

```vue
<script setup lang="ts">
import Button from '@cypress-design/vue-button'
</script>

<template>
  <div class="bg-white rounded">
    <div class="p-4">
      Lorem ipsum dolor sit amet, consectetur adipiscing elit.
    </div>
    <div class="p-4 flex flex-wrap gap-4 border-t border-t-gray-50">
      <Button variant="outline-indigo" size="32">Primary Action</Button>
      <Button variant="link" size="32">Cancel</Button>
      <div class="md:flex-grow" />
      <Button variant="link" size="32">Give feedback</Button>
    </div>
  </div>
</template>
```
