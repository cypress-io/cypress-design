---
'@cypress-design/constants-spec-results': major
'@cypress-design/react-spec-results': major
---

Adding SpecResults component — a run's per-status spec breakdown (failed/errored/passed/skipped/remaining) as filterable pills plus a proportional progress bar. React only for now; Vue is a planned fast-follow.
