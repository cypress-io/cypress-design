---
"@cypress-design/icon-registry": minor
"@cypress-design/react-icon": minor
"@cypress-design/vue-icon": minor
---

Add `action-test-replay` at 12px and 24px (previously 16px only), and add an opt-in `icon-light` disc behind the glyph at all three sizes. The disc is transparent by default — pass `fillColor` to show it — so existing bare usages are unchanged.
