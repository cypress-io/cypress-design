import { ArgsTable, Canvas, Meta, Story, Description } from '@storybook/addon-docs'
import { version } from "./package.json"
import changelog from "./CHANGELOG.md?raw"

import Carousel from './Carousel.vue'

import CarouselStory from './Carousel.rootstory'

<h1>Carousel<span className="text-lg font-normal"> - v{version}</span></h1>

<Meta title="Carousel" component={Carousel} parameters={{
    design: {
      type: "figma",
      url: null,
    }
  }} />

<Canvas withSource="none">
  <Story name="Carousel">
    {CarouselStory()}
  </Story>
</Canvas>

<ArgsTable />

<Description>
  {changelog.replace(/^# .+/, '## Changelog')}
</Description>

