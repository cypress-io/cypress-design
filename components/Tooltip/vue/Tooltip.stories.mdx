import { ArgsTable, Canvas, Meta, Story, Description } from '@storybook/addon-docs'
import { version } from "./package.json"
import changelog from "./CHANGELOG.md?raw"

import Tooltip from './Tooltip.vue'

import TooltipStory from './Tooltip.rootstory';

<h1>Tooltip<span className="text-lg font-normal"> - v{version}</span></h1>

<Meta 
  title="Tooltip" 
  component={Tooltip} 
  argTypes={{
    color: {
      options: ['light', 'dark'],
      control: { type: 'select' },
    },
    placement: {
      options: ['top', 'bottom', 'left', 'right'],
      control: { type: 'select' },
    },
  }}
  parameters={{
    design: {
      type: "figma",
      url: 'https://www.figma.com/file/1WJ3GVQyMV5e7xVxPg3yID/Design-System?node-id=893%3A0',
    }
  }}
/>

A tooltip is used to provide more information to help a user complete a task.

<Canvas withSource="none">
  <Story name="Tooltip">
    {TooltipStory()}
  </Story>
</Canvas>

<ArgsTable />

<Description>
  {changelog.replace(/^# .+/, '## Changelog')}
</Description>

